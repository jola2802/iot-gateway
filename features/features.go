package features

import (
	"database/sql"

	"github.com/gopcua/opcua"
)

// StartImageCapture initiates the image capturing process
func startImageProcessing(client *opcua.Client, db *sql.DB) {
	// ...
	// StartImageCapture(client)
}
